services:
  imagetagger:
    build:
      context: .
      dockerfile: Dockerfile
    runtime: nvidia          # required for Jetson GPU access via NVIDIA Container Runtime
    network_mode: host
    environment:
      - PORT=9100
      - RAM_CHECKPOINT=/app/ram_plus_swin_large_14m.pth
      - NVIDIA_VISIBLE_DEVICES=all
    restart: unless-stopped

