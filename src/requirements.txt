# Core server
flask>=3.0

# Image processing
pillow>=12.0  # 11.3+ has built-in AVIF support; no plugin needed
pyvips         # fallback decoder for AVIF files with malformed EXIF metadata

# Deep-learning backend
# In the JetPack/L4T Docker image, torch and torchvision are pre-installed with
# Jetson CUDA support — do NOT reinstall from PyPI (wrong architecture).
# For local development outside Docker, install manually:
#   Jetson: use the wheel bundled with your JetPack SDK
#   x86 CPU: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# torch>=2.0
# torchvision>=0.15

# RAM++ (recognize-anything) dependencies not pulled in automatically
scipy
fairscale>=0.4.4,<0.5.0

# Florence-2 and SigLIP
# Upper bound: Florence-2's remote config code accesses forced_bos_token_id
# before parent __init__ sets it, which breaks on transformers >= 4.47.
transformers>=4.53
timm
einops        # required by Florence-2's vision encoder
sentencepiece # required by SigLIP's tokenizer
protobuf      # required by SigLIP's tokenizer
safetensors   # required to load Florence-2 and SigLIP model weights
accelerate    # used by transformers internals during model loading

# NLP
spacy>=3.8.0
spacy[cuda12x]  # cupy for GPU support (matches CUDA 12.x on Jetson)
en_core_web_trf @ https://github.com/explosion/spacy-models/releases/download/en_core_web_trf-3.8.0/en_core_web_trf-3.8.0-py3-none-any.whl

# Production server
gunicorn>=21.0
